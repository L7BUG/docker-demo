services:
    kafbat-ui:
        container_name: kafbat-ui
        image: ghcr.io/kafbat/kafka-ui:latest
        ports:
            - 38080:8080
        environment:
            DYNAMIC_CONFIG_ENABLED: 'true'